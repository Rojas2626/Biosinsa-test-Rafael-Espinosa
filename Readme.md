Como usar:

Crea la base de datos mysql con el backup.
Crea el archivo .env en la carpeta raiz y agrega estas variables de entorno con sus datos:

DB_HOST=
DB_USER=
DB_PASS=
DB_NAME=

ejecuta el programa con el comando  "php -S localhost:8000" en la terminal.